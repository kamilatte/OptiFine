package notch.net.optifine.shaders.config;

import net.optifine.shaders.config.HeaderLineVariable;

public class HeaderLineUniform extends HeaderLineVariable {
  public HeaderLineUniform(String name, String text) {
    super("uniform", name, text);
  }
}
